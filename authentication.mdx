---
title: Authentication
description: Authenticating requests with an API key
---

# Authentication

All API requests require a `Bearer` token in the `Authorization` header.

```
Authorization: Bearer <token>
```

## API Keys

API keys are long-lived tokens scoped to a specific user. Use these for
server-to-server integrations, scripts, and automation.

```
Authorization: Bearer ak_live_a1b2c3d4e5f6_AbCdEfGhIjKlMnOpQrStUvWxYz012345
```

### Key format

Keys follow the pattern `ak_live_<keyId>_<secret>`:
- `ak_live_` — prefix identifying the token as an API key
- `<keyId>` — 12-character identifier
- `<secret>` — 32-character secret (hashed before storage, never stored in plain text)

### Getting a key

Generate and manage API keys from the Aurora dashboard — open the profile
menu and go to **Settings → API Keys**. The raw key is shown **once** when
it's created, so copy it immediately and store it securely.

### Revoking a key

Revoke a key any time from **Settings → API Keys** in the dashboard.
Revocation is permanent and immediate — any requests using the key will
fail right away.

## Error responses

| Status | Body | Cause |
|--------|------|-------|
| `401` | `{"error": "Unauthorized"}` | No `Authorization` header |
| `401` | `{"error": "Invalid or revoked API key"}` | Bad or revoked API key |
